# when deploying this app to azure, you first need to build project with vite

```sh
npm run build
```

then check in code. That will trigger github action to deploy code to azure static web site.

# to start application

```sh
npm run dev
```
